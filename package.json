{
  "name": "zaira-fatima-portfolio",
  "version": "1.0.0",
  "description": "Portfolio website and AI automations portal",
  "scripts": {
    "dev": "npx netlify dev --no-open",
    "deploy": "npx netlify deploy",
    "deploy:prod": "npx netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^26.0.2"
  }
}
